# Server-side API key from https://developers.faceit.com (Studio > your app > API Keys)
FACEIT_API_KEY=

# FACEIT nickname of the player to show on overlays (can be overridden per-overlay via ?nickname=)
DEFAULT_NICKNAME=

# Port the local overlay server runs on
PORT=4173
